'use script';

/*describe('utsHelps.login module', function () {
	beforeEach(module('utsHelps.login'), module('helpsRestfulServices'), module('utsHelps.auths'));
	var $scope, loginControl;
	beforeEach(inject(function($rootScope, $controller){
		$scope = $rootScope.$new();
		$scope.globals = {};
		loginControl = $controller('loginCtrl', {
			$scope:$scope,
		});
	}));
	describe('login controller', function () {
		it('should exist and when injected should load', inject(function($controller, $scope, $rootScope,
			_AUTH_EVENTS_, _AuthService_) {
			var loginControl = $controller('loginCtrl', {
				$scope: $scope,
				$rootScope: $rootScope,
				'AUTH_EVENTS': _AUTH_EVENTS_,
				AuthService: _AuthService_
			});
			expect(loginControl).toBeDefined();
		}));
	});

});*/